#include <iostream>
using namespace std; 


int main(){
    int n, answer;     
    cin >> n; 

    /* Pseudo Code 
    MenOfPassion(A[], n) {
        sum <- 0;                         //대입(초기화) O(1)
        for i <- 1 to n                   //반복 O(n)
            sum <- sum + A[i]; # 코드1    //덧셈O(1), 배열 접근 O(1), 대입 O(1)
        return sum;                       // 반환 O(1)
    }
    */
    cout << n << "\n";
    cout << 1 << "\n";
    
}